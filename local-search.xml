<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>重温C语言（一） 重新理解C语言中的声明语句</title>
    <link href="/2024/08/09/Review-C-1/"/>
    <url>/2024/08/09/Review-C-1/</url>
    
    <content type="html"><![CDATA[<blockquote><p>「万物之始，大道至简，衍化至繁。」</p><p>​                                              —— 老子《道德经》</p></blockquote><p>最近在重温C语言，重温过程中有不少新的心得体会，遂开启「重温C语言」系列。<del>也是自2017年12月有正式域名以来第一次正式写博客（啊这</del></p><p>本篇从一种新的角度来理解C语言中的<strong>声明语句</strong>，总结起来就一句话：</p><ul><li>C语言中的声明语句不宜理解为【类型名】+【变量名】，而应当理解为【朴素类型名】+【表达式】。</li></ul><span id="more"></span><h2 id="通常理解的声明语句"><a href="#通常理解的声明语句" class="headerlink" title="通常理解的声明语句"></a>通常理解的声明语句</h2><h3 id="基本声明语句"><a href="#基本声明语句" class="headerlink" title="基本声明语句"></a>基本声明语句</h3><p>在C语言中，基本声明语句大致可以分为三类：普通变量声明、函数声明以及数组声明。</p><p>普通变量的声明格式为：<code>类型 变量名;</code>，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> i;               <span class="hljs-comment">// 声明了一个int类型的变量i</span><br><span class="hljs-type">char</span>* p;             <span class="hljs-comment">// 声明了一个指向char类型的指针变量p</span><br><span class="hljs-type">float</span>** fp;          <span class="hljs-comment">// 声明了一个指向float* 类型的指针变量p（二重指针）</span><br><span class="hljs-type">float</span> a1, a2, a3;    <span class="hljs-comment">// 一条语句中可以声明多个变量</span><br><span class="hljs-type">double</span> score = <span class="hljs-number">1.0</span>;  <span class="hljs-comment">// 声明的同时可以对变量进行初始化</span><br><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">S</span> &#123;</span> <span class="hljs-type">int</span> value; <span class="hljs-type">char</span>* name; &#125;;<br><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">S</span> <span class="hljs-title">s1</span>;</span>    <span class="hljs-comment">// 对于结构体类型的变量亦是如此 （C语言声明结构体变量时需要加struct关键字）</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">S</span>* <span class="hljs-title">sp</span>;</span>   <span class="hljs-comment">// 结构体指针变量</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> &#123;</span> <span class="hljs-type">float</span> score; <span class="hljs-type">int</span> num; &#125; s2;  <span class="hljs-comment">// 可以声明匿名结构体变量，此时的【类型】部分为整个结构体的定义</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Q</span> &#123;</span> <span class="hljs-type">int</span> v; &#125; q1;               <span class="hljs-comment">// 当然，具名的结构体也可以在定义结构体时声明变量</span><br></code></pre></td></tr></table></figure><p>函数的声明的基本格式为：<code>返回类型 函数名(参数列表);</code>，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span>;  <span class="hljs-comment">// 声明了一个返回值为int、接收两个int类型参数的函数</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">sub</span><span class="hljs-params">(<span class="hljs-type">int</span>, <span class="hljs-type">int</span>)</span>;      <span class="hljs-comment">// 形参在声明时可以不写出具体名称</span><br><span class="hljs-type">void</span> <span class="hljs-title function_">foo</span><span class="hljs-params">(<span class="hljs-type">float</span> c)</span>;      <span class="hljs-comment">// void 表示函数什么都不返回</span><br><span class="hljs-type">void</span> <span class="hljs-title function_">f1</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;          <span class="hljs-comment">// 参数列表为void代表函数不接收任何参数</span><br><span class="hljs-type">void</span> <span class="hljs-title function_">f2</span><span class="hljs-params">()</span>;              <span class="hljs-comment">// 参数列表为空表示函数接收参数的个数不确定</span><br><span class="hljs-type">char</span>* <span class="hljs-title function_">f3</span><span class="hljs-params">()</span>;             <span class="hljs-comment">// 返回值为char指针类型的函数</span><br><span class="hljs-keyword">struct</span> S <span class="hljs-title function_">f4</span><span class="hljs-params">()</span>;          <span class="hljs-comment">// 返回结构体变量的函数</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">f5</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> S* p)</span>;    <span class="hljs-comment">// 接收一个结构体指针的作为参数的函数</span><br></code></pre></td></tr></table></figure><p>数组声明的基本格式为：<code>元素类型 变量名 [当前维度的长度] ... ;</code>，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> arr1[<span class="hljs-number">10</span>];                 <span class="hljs-comment">// 声明了一个int类型的数组，长度为10</span><br><span class="hljs-type">int</span> arr2[] = &#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;;       <span class="hljs-comment">// 如果数组长度为空，则在声明时必须进行初始化</span><br><span class="hljs-type">char</span> arr3[<span class="hljs-number">10</span>][<span class="hljs-number">5</span>];             <span class="hljs-comment">// 二维数组的声明，10行 5列</span><br><span class="hljs-type">float</span> arr4[][<span class="hljs-number">5</span>] = &#123; ... &#125;;    <span class="hljs-comment">// 二维数组的声明，第一维长度可以为空，但必须初始化</span><br><span class="hljs-type">char</span>* arr5[<span class="hljs-number">8</span>];                <span class="hljs-comment">// 指针数组（数组内的元素为char指针类型）</span><br><span class="hljs-type">double</span> arr6[<span class="hljs-number">3</span>][<span class="hljs-number">3</span>][<span class="hljs-number">3</span>];         <span class="hljs-comment">// 更高维的数组声明</span><br></code></pre></td></tr></table></figure><h3 id="函数指针的声明"><a href="#函数指针的声明" class="headerlink" title="函数指针的声明"></a>函数指针的声明</h3><p>关于什么是函数指针，本文不作探讨，仅对函数指针的<strong>声明</strong>进行探讨。</p><p>函数指针的声明可以参考函数的声明，将函数声明中的变量名前面加*号（代表是函数指针），并用圆括号括起（以区分<strong>函数指针</strong>和<strong>返回指针的函数</strong>），就可以将函数的声明变为的函数指针的声明。例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span>, <span class="hljs-type">int</span>)</span>;           <span class="hljs-comment">// 这是一个函数的声明语句</span><br><span class="hljs-type">int</span> (*fp)(<span class="hljs-type">int</span>, <span class="hljs-type">int</span>);         <span class="hljs-comment">// 这是一个函数指针的声明，名为fp，与函数add具有相同的参数列表与返回值</span><br>fp = &amp;add;                   <span class="hljs-comment">// 将函数add取地址赋值给fp</span><br>fp = add;                    <span class="hljs-comment">// 与第3行作用相同，但第3行更符合语义</span><br>fp = *add;                   <span class="hljs-comment">// 与第3行作用相同，但一般不这么写</span><br>fp = *******add;             <span class="hljs-comment">// 与第3行作用相同，一般脑子抽了会这么写</span><br><br><span class="hljs-type">int</span> result;<br>result = (*fp)(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>);       <span class="hljs-comment">// 对fp取地址得到函数add，然后进行调用</span><br>result = fp(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>)           <span class="hljs-comment">// 上一行更简洁的写法（算是C语言为数不多的语法糖）</span><br><br></code></pre></td></tr></table></figure><h2 id="从新的角度理解声明语句"><a href="#从新的角度理解声明语句" class="headerlink" title="从新的角度理解声明语句"></a>从新的角度理解声明语句</h2><p>从一个例子开始：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span>* p1, p2;<br></code></pre></td></tr></table></figure><p>请问，<code>p1</code>、<code>p2</code>分别是什么类型？可能会有人认为<code>p1</code>和<code>p2</code>都是是指针类型，但其实<code>p1</code>是指向<code>int</code>的指针类型，即<code>int *</code>类型；<code>p2</code>是<code>int</code>类型。</p><p>这是一个C语言初学者常犯的错误，如果我们认为C语言的声明语句就是【类型名】+【变量名】，那么对于上面的例子我们就会自然的认为<code>p1</code>、<code>p2</code>都是<code>int*</code> 类型。其实这样理解C语言中的声明语句是不准确的。更准确的说法应当为：</p><ul><li>【朴素类型名】 +【表达式】</li></ul><p>其中<strong>朴素类型名</strong>包括C语言中的基本类型（如：<code>int</code>、<code>char</code>、<code>unsigned long</code>等）、结构体类型、共用体类型以及由<code>typedef</code>定义的类型。但是不包括由上述类型派生出的<strong>指针</strong>类型。</p><p>其后的<strong>表达式</strong>为包含一个合法标识符的表达式，表达式中只允许下述操作：解引用<code>*</code>、函数调用<code>(...)</code>、数组取值<code>[..]</code>。</p><p>二者结合起来，声明语句的含意为对标识符执行表达式中的操作后，得到的值的类型为【朴素类型名】代表的类型。</p><p>例如上面给的例子：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span>* p1, p2;<br></code></pre></td></tr></table></figure><p>其实应当理解为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *p1, p2;<br></code></pre></td></tr></table></figure><ul><li>对<code>p1</code>取地址得到<code>int</code>类型，所以<code>p1</code>是指向<code>int</code>类型的指针。<code>p2</code>不执行任何操作，就是<code>int</code>类型。</li></ul><p>那么，有了新的理解，我们也可以对指针、函数、数组、函数指针等的声明语句进行重新解释。例如</p><ol><li><p>指针声明</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *p, **p2, ***p3;<br></code></pre></td></tr></table></figure><ul><li><code>*p1</code>是<code>int</code>类型。对<code>p1</code>解引用得到<code>int</code>类型，所以<code>p1</code>是指向<code>int</code>类型的指针</li><li><code>**p2</code>是<code>int</code>类型。对<code>p2</code>两次解引用得到<code>int</code>类型，所以<code>p2</code>是指向<code>int</code>类型的二重指针</li><li><code>***p3</code>是<code>int</code>类型。对<code>p3</code>三次解引用得到<code>int</code>类型，所以<code>p3</code>是指向<code>int</code>类型的三重指针</li></ul></li><li><p>函数声明</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span><br></code></pre></td></tr></table></figure><ul><li><code>add(int a, int b)</code>的类型是<code>int</code>。对<code>add</code>进行函数调用得到的值是<code>int</code>，所以<code>add</code>是一个返回<code>int</code>类型的函数，且接收参数列表为<code>(int a, int b)</code>。</li></ul></li><li><p>函数指针与返回指针的函数</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *<span class="hljs-title function_">f1</span><span class="hljs-params">(<span class="hljs-type">int</span>)</span>;<br><span class="hljs-type">int</span> (*f2)(<span class="hljs-type">int</span>);<br></code></pre></td></tr></table></figure><ul><li>第一行：<code>*f1(int)</code>是<code>int</code>类型，根据C语言的运算符优先级，函数调用<code>()</code>优先于解引用<code>*</code>，因此<code>f1</code>首先可以进行函数调用，调用结果进行解引用得到<code>int</code>类型，因此<code>f1</code>是一个返回值为<code>int *</code>类型的函数。</li><li>第二行：<code>(*f2)(int)</code>是<code>int</code>类型，由于括号内优先计算，因此<code>f2</code>首先可以解引用，然后进行函数调用，得到<code>int</code>类型。因此，<code>f2</code>是一个函数指针，指向一个返回值为<code>int</code>的函数。</li></ul></li><li><p>数组声明</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">char</span> str[<span class="hljs-number">100</span>];<br></code></pre></td></tr></table></figure><ul><li><code>str[100]</code>是<code>char</code>类型，对<code>str</code>进行下标运算得到<code>char</code>，因此<code>str</code>是<code>char</code>类型数组</li></ul></li><li><p>复杂类型变量 &#x2F; 函数声明</p><p>基于新的理解我们很容易就可以看懂复杂类型变量的声明，如下面的例子，<code>fp</code>是什么类型？</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *(*(*fp)(<span class="hljs-type">char</span>* str))[<span class="hljs-number">10</span>];<br></code></pre></td></tr></table></figure><p>可以对该语句<strong>由内而外</strong>进行拆解：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *(*(*fp)(<span class="hljs-type">char</span>* str))[<span class="hljs-number">10</span>];<br>       (*fp)                   <span class="hljs-comment">// fp首先可以解引用，所以fp是一个指针</span><br>       (*fp)(<span class="hljs-type">char</span>* str)        <span class="hljs-comment">// fp解引用的结果可以进行调用，是一个函数</span><br>                               <span class="hljs-comment">// 所以fp是函数指针</span><br>     (*(*fp)(<span class="hljs-type">char</span>* str))       <span class="hljs-comment">// 该函数的返回值可以进行解引用，所以fp指向的函数</span><br>                               <span class="hljs-comment">// 返回值是指针</span><br>     (*(*fp)(<span class="hljs-type">char</span>* str))[<span class="hljs-number">10</span>]   <span class="hljs-comment">// 该指针指解引用的结果可以进行下标运算</span><br>                               <span class="hljs-comment">// 所以该指针指向一个数组</span><br>    *(*(*fp)(<span class="hljs-type">char</span>* str))[<span class="hljs-number">10</span>]   <span class="hljs-comment">// 数组的元素可以解引用，所以数组是指针数组</span><br><span class="hljs-type">int</span> *(*(*fp)(<span class="hljs-type">char</span>* str))[<span class="hljs-number">10</span>];  <span class="hljs-comment">// 数组元素解引用后是int类型，所以数组是int指针数组</span><br></code></pre></td></tr></table></figure><p>最终可得知：</p><ul><li><code>fp</code>是一个<strong>函数指针</strong>，函数接收一个<code>char *</code>类型的参数<code>str</code>，函数的返回值是一个指向<strong>数组</strong>的指针，<strong>数组</strong>的元素为指向<code>int</code>类型的指针</li></ul><p>p.s.  C语言中不允许函数返回数组，因为：</p><ul><li><p>C语言中不支持将一个数组赋值给另一个数组</p></li><li><p>数组其实相当于一块内存，内存的拷贝是很消耗性能的，不如直接返回指针</p></li><li><p>不允许函数作为返回值亦是同理，所以使用函数指针</p></li></ul></li></ol><h2 id="typedef-关键字"><a href="#typedef-关键字" class="headerlink" title="typedef 关键字"></a><code>typedef</code> 关键字</h2><p><code>typedef</code>关键字是为了简化声明语句，增强类型的可读性，其作用可以概括为：</p><ul><li><code>typedef</code> 后面必须是一个合法的声明语句</li><li><code>typedef</code>会将声明语句中的标识符由<strong>变量名</strong>（或函数名）转变为<strong>类型名</strong>。</li></ul><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *p;                  <span class="hljs-comment">// 声明了一个int类型指针</span><br><span class="hljs-comment">// 前面加 typedef 关键字</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-type">int</span> *int_ptr;    <span class="hljs-comment">// int_ptr 与 p具有相同的声明形式，所以int_ptr代表指向int类型的指针，即p的类型 </span><br>int_ptr p2;              <span class="hljs-comment">// 声明了一个与p类型相同的变量p2</span><br><br><span class="hljs-comment">// 使用typedef简化结构体变量、指针、函数的声明</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">S</span> </span><br><span class="hljs-class">&#123;</span><br>    <span class="hljs-type">int</span> value;<br>&#125; S, SPtr;                    <span class="hljs-comment">// 将结构体struct S简化为S，struct S *简化未SPTR</span><br><br>S s1 = &#123; .value = <span class="hljs-number">10</span> &#125;; <span class="hljs-comment">// 可以值接用S声明结构体变量</span><br>S *sp = &amp;s1;            <span class="hljs-comment">// 可用于指针声明</span><br>S <span class="hljs-title function_">foo</span><span class="hljs-params">(SPtr s)</span>;            <span class="hljs-comment">// 可用于函数声明</span><br></code></pre></td></tr></table></figure><p>函数指针的例子：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span> &#123;<br>    <span class="hljs-keyword">return</span> a + b;<br>&#125;<br><br><span class="hljs-keyword">typedef</span> <span class="hljs-title function_">int</span> <span class="hljs-params">(*fpType)</span><span class="hljs-params">(<span class="hljs-type">int</span>, <span class="hljs-type">int</span>)</span>;  <span class="hljs-comment">// fpType现在是一个类型，代表函数指针类型，接收两个int类型传参，返回值为int</span><br>fpType fp = &amp;add;                 <span class="hljs-comment">// 使用fpType定义了一个函数指针变量fp，并将add的地址赋给fp</span><br>                                  <span class="hljs-comment">// 上述表达式就等价于 int (*fp)(int, int) = &amp;add;</span><br><br><span class="hljs-type">int</span> r = fp(<span class="hljs-number">2</span>, <span class="hljs-number">4</span>);                 <span class="hljs-comment">// 可以像正常函数指针一样进行调用</span><br></code></pre></td></tr></table></figure><p>我们还可以把前文的复杂声明使用<code>typedef</code>进行优化：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-comment">// 原始声明语句</span><br><span class="hljs-type">int</span> *(*(*fp)(<span class="hljs-type">char</span>*))[<span class="hljs-number">10</span>];<br><br><span class="hljs-comment">// 使用typedef进行优化</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-type">int</span> *Arr[<span class="hljs-number">10</span>];<br><span class="hljs-keyword">typedef</span> Arr *ArrPtr;<br><span class="hljs-keyword">typedef</span> ArrPtr <span class="hljs-title function_">Func</span><span class="hljs-params">(<span class="hljs-type">char</span>*)</span>;<br><span class="hljs-keyword">typedef</span> Func *FuncPtr;<br><br>FuncPtr fpNew;<br></code></pre></td></tr></table></figure><p>此时，<code>fpNew</code>与<code>fp</code>具有相同的类型，但是可读性<code>fpNew</code>明显好于<code>fp</code>。</p><p>从上述示例中可以看到，使用<code>typedef</code>可以大大简化声明语句，并且使声明语句中的类型更加明确和可读。</p><p>p.s. 当<code>typedef</code>用于函数声明就会引发一些不明觉厉<del>但没什么卵用</del>的操作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-comment">// int add(int a, int b);         // 函数声明</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-type">int</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span>;    <span class="hljs-comment">// add变成了一个函数类型，可以用于声明一个函数</span><br>add fadd1;                        <span class="hljs-comment">// 声明了一个函数fadd1，等价于：int fadd1(int a, int b);</span><br>fadd1(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>);                      <span class="hljs-comment">// 然后就可以使用了</span><br>add *fp;                          <span class="hljs-comment">// 可以用于声明函数指针，等价于 int (*fo)(int a, int b);</span><br><br><span class="hljs-comment">// ... 其他某处，fadd1函数定义</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">fadd</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span> &#123;<br>    <span class="hljs-keyword">return</span> a + b;<br>&#125;<br><br><span class="hljs-comment">// 不能用于函数定义，如下面的语句是不合法的</span><br><span class="hljs-comment">// add fadd1 &#123;</span><br><span class="hljs-comment">//     return a + b;</span><br><span class="hljs-comment">// &#125;</span><br><br></code></pre></td></tr></table></figure><h2 id="补充1：关于const说明符"><a href="#补充1：关于const说明符" class="headerlink" title="补充1：关于const说明符"></a>补充1：关于<code>const</code>说明符</h2><p>C语言中，<code>const</code>一般表示所修饰的变量是不可变的，但是在指针变量的声明中，可能会由于<code>const</code>在声明语句中未知的不同，而产生语义的不同。其实，包含<code>const</code>的声明语句也可以使用前文所述方法进行解释。比如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">const</span> <span class="hljs-type">int</span> *p1;        <span class="hljs-comment">// *p1 是const int，所以*p不可变，p指向的地址可变</span><br><span class="hljs-type">int</span> <span class="hljs-type">const</span> *p2;        <span class="hljs-comment">// *p2 是int const，与int const等价</span><br><span class="hljs-type">int</span> *<span class="hljs-type">const</span> p3;        <span class="hljs-comment">// p3 是const的，所以p3不可变，而解引用后*const p3是int</span><br>                      <span class="hljs-comment">// 所以p3指向的值可变</span><br><span class="hljs-type">const</span> <span class="hljs-type">int</span> *<span class="hljs-type">const</span> p4;  <span class="hljs-comment">// p4 是const的，所以p4不可变，而解引用后*const p4是const int</span><br>                      <span class="hljs-comment">// 所以p4指向的值也不可变</span><br></code></pre></td></tr></table></figure><p>那么，下面这句声明该如何理解呢？</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">const</span> <span class="hljs-type">int</span> *<span class="hljs-type">const</span> **<span class="hljs-type">const</span> *p1;<br></code></pre></td></tr></table></figure>    <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-eda0a050" role="button" aria-expanded="false" aria-controls="collapse-eda0a050">        <div class="fold-arrow">▶</div>点击查看答案      </div>      <div class="fold-collapse collapse" id="collapse-eda0a050">        <div class="fold-content">          <ul><li><code>p1</code>是一个四重指针，最终指向<code>int</code>类型</li><li><code>p1</code>, <code>**p1</code>是可变的</li><li><code>*p1</code>, <code>***p1</code>, <code>****p1</code>是不可变的</li></ul>        </div>      </div>    </div><h2 id="补充2：-复杂类型的类型名（用于强制类型转换）"><a href="#补充2：-复杂类型的类型名（用于强制类型转换）" class="headerlink" title="补充2： 复杂类型的类型名（用于强制类型转换）"></a>补充2： 复杂类型的类型名（用于强制类型转换）</h2><p>如果我们要对复杂类型的变量进行强制类型转换，在不使用<code>typedef</code>的情况下应该怎么做呢？</p><p>其实将复杂类型变量的声明语句中的变量名去掉，就是变量的完整类型名。如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> *(*(*fp)(<span class="hljs-type">char</span>*))[<span class="hljs-number">10</span>];                  <span class="hljs-comment">// fp的类型就是：int *(*(*)(char*))[10]</span><br>fp = (<span class="hljs-type">int</span> *(*(*)(<span class="hljs-type">char</span>*))[<span class="hljs-number">10</span>]) anotherFunc;   <span class="hljs-comment">// 可以进行强制类型转换</span><br></code></pre></td></tr></table></figure>]]></content>
    
    
    
    <tags>
      
      <tag>C语言</tag>
      
      <tag>重温C语言</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
